
#include "unity.h"

/* Per-exercise hooks: each glorp test file defines these; setup.c and
** test_runner.c call them. Declared here so every translation unit has a
** prototype (no implicit declarations under -Werror). */
void	glorpUp(void);
void	glorpDown(void);
void	glorpRun(void);
